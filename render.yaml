services:
  - type: web
    name: qseriesveo-api
    env: node
    region: us-east-1
    buildCommand: cd server && npm install
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
